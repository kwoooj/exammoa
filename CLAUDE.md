# exammoa — Claude Code 작업 지침

여러 기관에 흩어진 시험의 원서접수·시험·발표 일정을 **월간 캘린더로 탐색하고 공식 접수처로 내보내는** 정적 사이트. 백엔드 없음, 하루 1회 배치로 정적 JSON 생성, 빌드 시 68개 페이지 사전 렌더.

읽는 순서: `README.md` → `docs/데이터-수집-활용.md`(수집 경로와 다음 계획) → `docs/reference/데이터-스키마.md`(필드 계약).

---

## 명령

```bash
npm run collect        # API·크롤 수집 → data/published/*.json  (QNET_KEY 필요)
npm run publish:data   # data/published/ → public/data/  (화면이 읽는 경로)
npm run dev            # predev 가 publish:data 를 먼저 돌린다
npm test               # node --test. scripts/**/*.test.mjs + src/**/*.test.ts
npm run typecheck      # 오류 0 을 유지한다
npm run check:seed     # 시드 양방향 무결성 (네트워크 없음, 제일 빠름)
npm run check          # PR 전 체크 한 방 = 시드 + test + typecheck + 산출물 점검
npm run probe          # Q-Net API 진단
npm run probe:crawl    # 기관 페이지 크롤링 가능성 진단

npm run collect -- --replay=2026-08-13   # 그날 원본에 지금 파서 재실행 (네트워크·키 없음)
```

수집 산출물은 `data/published/` 다. `build/` 는 진단·스크래치 전용이고 `.gitignore` 대상이다.

`QNET_KEY` 는 `.env` 에 있고 **URL 인코딩된 상태(`%3D%3D`)로 그대로** 넣는다. `URLSearchParams` 로 감싸면 `%` 가 이중 인코딩되어 인증이 깨진다.

---

## 반드시 지킬 것

**1. 일정의 주체는 종목이 아니라 시행그룹이다.**
`Session` 은 `groupId` 를 갖는다. `examSlug` 로 되돌리지 말 것. 실측: 47종목의 일정이 실제로 7가지뿐이라 종목별로 그리면 캘린더에 같은 막대가 29줄 반복된다. 접기는 sessions 를 순회해서 얻고 막대 id 에 slug 를 넣지 않는다 — 그래야 중복이 구조적으로 불가능하다. 접힌 종목은 막대의 속성(examSlugs)이 되고 상세 패널이 전부 나열한다.

**2. `groupId` 는 선언하고, 동일성은 검증한다.**
`data/groups.seed.json` 에 사람이 선언하고 `foldGroups()` 가 실제 일정이 같은지 확인한다. 일정 지문을 그룹 id 로 쓰면 회차가 바뀔 때마다 id 가 흔들려 URL 공유와 `ExamPlan.sessionId` 가 깨진다. 갈리면 조용히 하나를 고르지 말고 `meta.groupSplits` 에 남기고 종료코드 1.

**3. `robots.txt` 를 어기지 않는다.**
`dataq.or.kr` · `cq.or.kr` · `opic.or.kr` 은 `Disallow: /` 다. 대한상의 `/kor/` 도 금지다 (FR-DAT-11 완료조건). 자동 수집 대상만 `sourceUrl`, 링크 전용은 `agencyUrl` — 필드 이름으로 구분을 강제한다. 새 수집 대상을 추가할 때는 `npm run probe:crawl` 로 먼저 확인한다.

**4. 날짜를 추측해서 만들지 않는다.**
파싱 실패는 `null` 이고 이벤트를 만들지 않는다. `미정`·`-` 에 날짜를 부여하면 사용자가 접수를 놓친다. `2026.02.29` 같은 값은 `new Date` 가 3/1 로 굴려버리므로 직접 검산한다.

**5. 상시시험(rolling)에 막대를 그리지 않는다.**
컴활·워드·OPIc·ITQ 는 확정 회차가 없다. `mode: 'rolling'`, `events: []`, `rollingRule` 로 규칙 카드만 낸다. `status: 'tbd'`(미공고)와 혼동하지 말 것 — 둘 다 `events` 가 비지만 완전히 다른 정보다.

**진짜 버그는 튀어나온 막대가 아니다.** `events` 가 비어 있어 순진한 구현은 **우연히** 막대를 안 그린다. 나올 버그는 **없는 규칙 카드** 다 — 컴활을 검색했더니 아무 설명 없는 빈 달력이 나온다. 테스트는 "막대가 없다" 가 아니라 "규칙 카드가 있다" 를 확인한다.

**막대를 안 그리는 것과 고를 수 없는 것은 다르다.** 그룹마다 이벤트 없는 회차 하나를 두어 P2 계획이 가리킬 자리를 준다. 사용자가 **예약한 날짜**를 넣으면 D-Day 가 생긴다 — 사용자의 사실이지 우리가 만든 날짜가 아니다. 반면 **접수 마감은 규칙에서 계산하지 않는다.** "약 3주 전" 을 날짜로 바꾸는 순간 규칙 4 위반이고, 틀린 마감일 하나가 시험 하나를 날린다.

**6. 사람이 고치는 파일과 기계가 쓰는 파일을 섞지 않는다.**
`data/*.seed.json` · `data/manual-schedules.json` 은 사람. `data/published/` · `data/archive/` 는 기계(하루 1회 배치가 커밋한다). 수집 결과를 시드에 자동 커밋하면 cron 이 사람의 수정을 덮는다.

**시드 두 개는 서로를 가리키므로 양방향으로 검사한다** (`npm run check:seed`). 한 방향만 보면 반대쪽 구멍이 조용히 남는다 — 그룹만 있고 종목이 없으면 화면에 아무것도 안 나오는데 수집은 성공하고 게이트는 초록이다. 이 형태를 **네 번** 손으로 찾은 뒤에야 코드로 막았다.

**7. 부분 실패로 전체를 멈추지 않는다.**
소스 하나가 죽어도 나머지로 빌드가 성공해야 한다 (FR-DAT-06). 산출물을 먼저 쓰고 그다음 종료코드로 알린다. 낡은 데이터를 **낡았다고 밝히면서** 보여주는 것이 사라지는 것보다 낫다 (FR-DAT-07).

**8. 종료코드 1 을 뭉치지 않는다.**
`collect.mjs` 는 여러 이유로 1 을 낸다. **소스 실패**는 직전 값이 `stale` 로 유지되므로 산출물이 유효하다 — 커밋해야 폴백이 저장소에 남는다. **그룹 갈림**은 `groupId` 가 바뀌므로 커밋하면 접기 단위가 흔들린다 — 어제 한 막대였던 것이 오늘 셋으로 갈라져 캘린더가 다른 화면이 된다. 커밋하지 않는다. 판정은 `scripts/ci-gate.mjs` 한 곳에만 둔다. 워크플로 YAML 안에서 다시 판정하지 말 것 (테스트할 수 없다).

**9. 테스트는 저장소에 있는 것만 읽는다.**
`build/` 는 `.gitignore` 대상이라 **CI 에 없다.** 픽스처를 거기서 읽고 없으면 넘어가는 테스트는 로컬에서만 돌고 CI 에서는 조용히 통과한다 — 실제로 16건이 그랬다. 고정 데이터는 테스트 파일 안에 두고, 값은 `data/archive/` 의 실측 스냅샷에서 옮긴다. `scripts/lib/test-hygiene.test.mjs` 가 이 규칙을 강제한다.

**10. 드리프트 기준선은 직전 1회가 아니다.**
크롤링은 터지지 않고 **어긋난다.** 사이트가 개편되면 파서가 성공적으로 0건을 돌려주고, 그 0건을 조용히 게시하는 것이 가장 위험한 실패다. 직전과 비교하면 **직전이 이미 깨져 있었을 때 깨진 값이 정상으로 승격된다** — 최근 30회 중위값을 쓴다(`lib/drift.mjs`). 임계를 좁히고 싶어지면 먼저 토익을 보라. 45→44→43 으로 정상적으로 줄어든다.

**11. 실패는 소스 단위로 굴러야 폴백이 작동한다.**
`health: 'ok'` 인 소스는 회차가 통째로 교체된다. 그래서 **종목 단위 실패를 소스 건강도에 반영하지 않으면** 절반이 빠진 결과가 정상으로 게시된다 (#18: 29/47 실패 → 그룹 3개·회차 49건 소멸, 종료코드 0). 허용치는 `lib/qnet.mjs` 의 `FAILURE_TOLERANCE`. 계정 단위 거절(한도·키·IP)은 즉시 소스 실패이고 **남은 종목을 호출하지 않는다.**

**12. 계산은 `src/lib/` 에 두고 `.tsx` 에는 배선만 남긴다.**
`node --test` 는 타입만 벗겨 `.ts` 를 직접 돌린다. **JSX 는 변환하지 못하므로 컴포넌트 안으로 들어간 계산은 영원히 검증되지 않는다.** 날짜·정렬·필터·좌표를 컴포넌트에서 계산하고 싶어지면 그것이 lib 로 갈 신호다. jsdom·vitest 를 들이지 않는다 — 하네스를 바꾸는 결정이지 편의가 아니다.

같은 이유로 `src/lib/monthbars.ts` 는 `types.ts` 를 import 하지 않는다. 도메인을 모르는 순수 기하라야 픽스처 한 줄로 테스트된다.

**13. 화면은 `today()` 를 직접 부르지 않는다.**
사전 렌더한 HTML 은 **빌드한 날의 오늘**로 그려져 있다. 컴포넌트 한 곳이라도 진짜 오늘을 직접 읽으면 그 부분만 서버 출력과 어긋나고, React 19 는 사전 렌더 트리를 **통째로 버린다**. `useToday()` 를 쓴다. 같은 이유로 첫 렌더의 주소도 페이로드에 실린 사전 렌더 경로여야 한다 — `/exams` 를 쿼리 없이 찍어 두고 `?status=open` 으로 하이드레이션하면 필터 초기화 버튼 하나 때문에 깨진다.

**14. 색은 어느 시험인지를, 모양은 일정 종류를 말한다.**
둘을 겹치면 색맹 사용자와 흑백 인쇄에서 접수와 시험이 같아진다 (화면정의 §3.2). 접수는 테두리, 추가접수는 점선, 시험은 채움, 발표는 점선 원이다. 시행그룹이 하나뿐인 화면에서는 색을 아예 쓰지 않는다 — 구분하지 않는 색은 장식이고 §13.2 가 금지한 쪽이다.

지난 막대에 `opacity` 를 쓰지 않는다. 다크 모드에서 밝은 잉크에 걸면 대비가 AA 아래로 떨어진다. 전용 토큰(`--bar-past`)을 쓴다.

---

## git 형상관리

**통합 브랜치는 `mvp` 다.** 기본 브랜치이고 PR 의 base 이며, `schedule` 이 발동하는 워크플로 파일도 여기 것을 읽는다. `main` 은 `mvp` 를 따라가는 별칭일 뿐이니 거기서 브랜치를 따지 않는다.

`mvp` 에 직접 커밋하지 않는다. 항상 이슈 → 브랜치 → 커밋 분리 → PR.

```bash
gh issue create --title "..." --body "..."
git switch -c <type>/<issue번호>-<요약>
# 논리 단위로 커밋을 쪼갠다
gh pr create --fill
```

**브랜치 이름**: `feat/` `fix/` `docs/` `chore/` `refactor/` + 이슈 번호. 예: `feat/12-stale-fallback`

**커밋 분리 기준** — "이 커밋만 되돌릴 수 있는가"로 판단한다.
- 데이터 시드 변경과 그것을 읽는 코드 변경은 나눈다
- 타입 계약 변경과 그에 맞춘 구현은 나눈다
- 버그 수정은 기능 변경과 섞지 않는다
- 문서 갱신은 별도 커밋 (리뷰어가 코드만 볼 수 있게)

**커밋 메시지**: 첫 줄은 한국어 명령형 요약 50자 내. 본문에는 **왜**를 쓴다. 무엇을 바꿨는지는 diff 가 말해준다.

```
그룹 접기 추가로 47종목을 7행으로 줄임

종목별로 행을 그리면 실측상 같은 막대가 29줄 반복된다.
groupId 는 시드에 선언하고 동일성은 수집 시점에 검증한다.
```

**PR 전 체크**: `npm run check` (test + typecheck + `groupSplitCount` 0). `.github/workflows/ci.yml` 이 PR 에서 같은 것을 돌린다.

`npm run collect` 는 CI 에서 돌지 않는다 — 외부 기관 사이트가 죽은 날 코드 리뷰가 막히면 안 된다. 수집 경로를 건드린 PR 은 손으로 한 번 돌려 보고 종료코드를 확인한다.

---

## 함정 (실측으로 확인된 것)

- **API 응답에 종목 식별자가 없다.** 요청 시 넣은 `jmCd` 로만 귀속 가능 → 종목별 루프가 불가피하다. 이 중복이 그룹 무결성 검사를 공짜로 준다.
- **잘못된 `jmCd` 는 오류 없이 엉뚱한 종목을 준다.** 화이트리스트로만 호출한다.
- **응답 0건은 오류가 아니다.** `1324`·`1325`·`0492`·`0493`·`0488`·`0483` 은 타기관 시행이라 정상적으로 빈 응답이다.
- **거절은 빈 응답과 생김새가 다르다.** 일일 한도를 넘기면 `header`/`body` 가 아니라 `OpenAPI_ServiceResponse.cmmMsgHeader` 로 온다 (`returnReasonCode: 22`). `extractItems()` 가 빈 배열을 돌려주므로 **"레코드 없음" 과 구분되지 않았고, 29종목이 `health:'ok'` · 종료코드 0 으로 조용히 사라졌다** (#18). 지금은 `lib/qnet.mjs` 의 `classifyResponse()` 가 갈라낸다. **새 오류 형태를 만나면 여기에 추가한다** — 호출부에서 0건으로 흘려보내지 말 것.
- **개발계정 한도는 일 1,000, 1회 수집이 47회 → 하루 21회가 상한.** 배치 1회는 안전하다. 손으로 여러 번 돌린 날은 `meta.failed` 를 확인한다.
- **새벽에 배치를 돌리지 않는다.** 04:58 KST 실행에서 `apis.data.go.kr` 47종목 전부 `fetch failed`, `historyexam.go.kr` 타임아웃이었는데 상용 도메인(toeic·kbskorean)은 멀쩡했다. 같은 날 17:02 KST 에는 둘 다 정상. cron 은 **07:10 KST** 다 (#24). 시각을 바꿀 때 이 실측을 지우지 말 것.
- **모든 fetch 에 타임아웃을 준다.** `call()` 에 없어서 엔드포인트가 죽은 날 종목마다 OS 연결 타임아웃(약 10.6초)을 기다려 8분 48초가 걸렸다. 연속 5종목 네트워크 실패면 남은 종목을 호출하지 않는다.
- **필기시험은 기간 시행(CBT)이다.** 정보처리기사 필기가 26일. 시험이 점이 아니라 막대이고, 기간끼리 겹쳐도 응시일을 고르면 피할 수 있어 `blocking` 에서 제외한다 (`isFlexible`, 4일 이상).
- **한 회차에 접수 구간이 2개다.** 정기접수 + 빈자리접수 → `reg` 의 `seq` 1·2.
- **과거 연도 조회가 안 된다.** `implYy=2024` 빈 응답. 매 수집 시 원본을 `build/raw/` 에 보존한다.
- **한국 시험표는 셀에 `M.D(요일)` 로 쓰고 연도는 제목에 한 번만 적는다.** 4자리 연도를 요구하는 정규식은 대부분의 표를 놓친다.
- **`res.text()` 는 charset 을 무시한다.** Fetch 명세상 언제나 UTF-8 로 디코드한다. 한국세무사회 페이지는 `charset=euc-kr` 을 헤더로 선언하는데도 그렇다 — 표 헤더가 깨져 `tableByHeader()` 가 아무것도 못 찾는다. 크롤 응답은 `lib/csv.mjs` 의 `decodeResponse()` 를 거친다.
- **연도가 페이지에 없어도 회차 표기에 있을 수 있다.** 리눅스마스터는 `'2026'` 이 한 번도 안 나오지만 `2601회` 의 앞 두 자리가 연도다. "연도 출처 없음" 으로 포기하기 전에 회차·제목·URL 을 본다.
- **공공데이터 CSV 는 EUC-KR 이고 따옴표 안에 콤마가 있다.** `readFile(path,'utf8')` 로 읽으면 헤더가 깨지고, `split(',')` 로 쪼개면 `시험장소` 의 콤마 때문에 컬럼이 밀려 마지막 칸에 `' 4층"'` 같은 값이 **조용히** 들어온다. `lib/csv.mjs` 를 쓴다.
- **CSV 의 시험명 표기가 연도에 따라 바뀐다.** `SQLD(국가공인 SQL 개발자)` → `SQL 개발자(SQLD)`. 매핑에 없는 이름은 조용히 버리지 않고 실패로 집계한다 — 표기가 또 바뀌면 종목이 사라지는 게 아니라 빨간불이 켜져야 한다.
- **낡음 판정은 소스별 주기로 한다.** 하나의 임계로 재면 연 1회 발행되는 CSV 가 붙는 순간 경고가 영구히 켜진다. 소스가 `staleAfterDays` 를 선언하고, 관측 시각도 `observedAt` 으로 직접 선언한다 — 파일을 읽은 시각을 `fetchedAt` 으로 쓰면 화면이 "최종 확인 오늘" 이라고 거짓말한다.
- **`robots.txt` 404 는 전면 허용이다** (RFC 9309). "판단 불가"로 처리하면 한능검처럼 값어치 있는 대상을 스스로 차단한다.
